# Local Postgres (see repo root docker-compose.yml)
DATABASE_URL="postgresql://app:app@localhost:5432/financial_center?schema=public"

# JWT signing (use a long random string in production)
JWT_SECRET="change-me-in-production-min-32-chars-long"

# API
PORT=3000
